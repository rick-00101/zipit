// PLACEHOLDER — overwritten per deployment by Zepit's orchestrator, which writes the
// resolved api URL in here after the api subdomain exists. Checked in only so the
// template runs standalone in local dev.
window.ZEPIT_CONFIG = {
  apiUrl: "http://localhost:3000",
  siteTitle: "Zepit Links",
  ownerName: "the team",
  theme: "dark"
};
