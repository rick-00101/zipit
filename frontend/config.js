// GENERATED FOR THE ZEROPS DEPLOYMENT of Zepit's own frontend.
// Same mechanism the archetype templates use: the api's subdomain is resolved after
// its first push, then written here before the frontend is pushed. Set apiUrl to ""
// to run same-origin against a local backend serving this folder.
window.ZEPIT_CONFIG = {
  apiUrl: "https://zepitapi-2a81-3000.prg1.zerops.app"
};
